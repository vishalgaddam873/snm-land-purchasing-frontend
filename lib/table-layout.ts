/** Sticky column headers — stays pinned at the top while the table body scrolls. */
export const stickyTableHeaderClassName =
  "sticky top-0 z-20 bg-card shadow-[inset_0_-1px_0_0_hsl(var(--border))]";
